# 变更记录

本文件记录织台对公开使用者有意义的变化。版本格式遵循语义化版本；预发布版本可能发生不兼容变更。

## Unreleased

### Planned

- 根据公开反馈继续收紧配置示例、适配器契约和安全边界。
- 为数据迁移、备份恢复和外置引擎兼容性补充更多测试。

## 0.1.0-alpha.1 - 2026-08-27

首个 Public Preview。

### Added

- React 工作台和 Electron 桌面外壳源码；
- 仅绑定回环地址的本地节点；
- SQLite 知识库、内容包、搜索、筛选、媒体预览和导出契约；
- 采集、分析、生成、通知、远程控制与发布的可选适配层；
- 来源白名单、固定服务命令、路径边界、可配置且配置后强制的 HMAC 校验，以及公开发布和部分服务控制的操作确认门；
- 中文优先的公开文档、社区健康文件和第三方许可证说明。

### Changed

- 将机器特定的组装说明改为可复现的 pnpm 快速开始；
- 明确区分仓库内可运行核心与用户自行安装的外置引擎；
- 默认把平台能力描述为预览、实验或未配置状态，不再暗示完整功能开箱即用；
- 对过时的内部交付文档进行删除、归档或泛化。

### Security

- 明确本地节点不得暴露到局域网或公网；
- 明确凭据、账号数据、真实素材和本机路径不得进入仓库；
- 公开发布和部分受管理服务操作使用显式确认；文档要求其他高影响自动化同样保留人工审核。
- 修复可省略签名头的收件入口降级、GET 深检触发、平台 URL 白名单绕过和成片归档竞态；
- 封面下载增加逐跳 SSRF、重定向、体积、Content-Type 与文件 magic 校验；
- 可执行 Release 资产和热更新要求 SHA-256，依赖审计在发布锁文件上为 0。
- 发布回执改用 SQLite 的操作系统级进程锁，进程异常退出后自动释放，避免陈旧锁清理竞态破坏幂等。

### Known limitations

- 主要开发验证环境为 macOS；
- 尚无稳定、签名的跨平台桌面发行包；
- 外置平台集成可能因账号、地区、上游版本、许可证或平台接口变化而不可用；
- 无 Origin 的本机客户端被视为同一用户信任边界；写接口尚未统一强制签名或确认；
- 尚未完成独立安全审计。
