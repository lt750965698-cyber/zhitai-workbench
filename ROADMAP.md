# 织台路线图

织台目前处于 Public Preview。这里记录的是优先方向，不是交付承诺；顺序会根据安全问题、平台变化、维护成本和社区反馈调整。

## 原则

- 本地优先，默认最小权限；
- 核心在没有平台账号和外置引擎时仍能运行；
- 外置引擎可替换、可禁用、可审计；
- 缺失数据明确标注，不生成伪结果；
- 公开发布保留人工审核，并逐步把统一授权与确认扩展到其他高影响动作；
- 不打包许可证不明确或不允许再分发的第三方源码。

## v0.1 Alpha：公开基线

- [x] 本地工作台、回环节点、SQLite 知识库和内容包契约；
- [x] 任务、事件、搜索、媒体预览和可替换适配器边界；
- [x] 来源白名单、路径检查、HMAC、人工确认门和脱敏文档；
- [x] macOS 与 Ubuntu CI、CodeQL、Dependabot 和密钥扫描；
- [x] 公开文档、社区文件、MIT 许可证和第三方声明；
- [x] 扩展无外置引擎的离线 E2E 与故障注入基线。

## v0.2 Alpha：可复现与可恢复

- [x] 加入可验证备份、隔离恢复、迁移预览和可恢复回滚；
- [x] 加入脱敏诊断快照、任务恢复和可观测性基线；
- [x] 加入统一内容生命周期契约和运营指标契约/只读报告；
- [x] 发布 Windows 10 22H2/Windows 11 x64 桌面公开预览构建（暂未代码签名）；
- [x] 建立 19 场景/352 断言的离线 E2E，并在 Windows 加入进程级断网护栏、在 Linux Docker 加入内核级断网；
- [ ] 为 macOS 和 Windows 桌面包建立稳定代码签名与公证流程；
- [ ] 继续扩展干净机器安装验证，提升键盘操作、无障碍、中文文案和英文入口。

## v0.3 Alpha：适配器与供应链

- 稳定适配器协议、版本协商、模拟器和离线契约测试；
- 为每个外置引擎公开权限、数据流、许可证和健康状态清单；
- 完善固定版本、校验和、更新预览、失败回滚与供应链证明；
- 加强第三方进程隔离、资源限制和故障降级；
- 继续禁止自动安装许可证不明确或分发受限的组件。

## v0.4 Alpha：安全硬化

- 统一本地写接口的认证、确认门、审计事件和错误码；
- 发布更完整的威胁模型与独立安全评审结果；
- 完善凭据生命周期、撤销、轮换和诊断泄漏响应；
- 对 Webhook、IPC、路径、下载、更新和发布门做模糊测试；
- 验证安全默认值、部署自检和恢复演练。

## Beta 候选条件

只有满足以下条件才会进入 Beta：

- 干净环境可以按文档启动核心；
- 迁移、备份和恢复经过自动化验证；
- 高影响操作范围已定义，且无法静默绕过统一授权与人工确认；
- 发行物不包含来源或许可证不清的第三方组件；
- 关键安全问题有明确的响应与升级机制；
- 核心接口和数据 schema 有兼容策略。

## 后续探索

- 更稳定的插件或适配器开发接口；
- 在 Windows 公开预览基线上继续改进跨平台本地节点与桌面外壳，但不会以牺牲安全边界为代价；
- 在明确授权、可撤销和可审计前提下扩展团队协作能力；
- 独立安全审计和更完整的供应链证明。

## 不在路线图中

- 绕过登录、验证码、访问控制、付费、版权保护或平台风控；
- 未经授权抓取私密数据、冒充账号、批量骚扰或自动公开发布；
- 把许可证不明确、限制再分发或来源不可核验的第三方源码和二进制打包进仓库；
- 将本地节点默认暴露到局域网或公网；
- 用“接口返回成功”代替平台端人工核验。

欢迎通过功能建议 Issue 说明具体用户问题、可验证结果、数据边界和人工降级路径。安全问题请按 [安全政策](SECURITY.md) 私下报告。
