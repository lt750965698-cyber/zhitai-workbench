# 安全政策

## 支持范围

织台仍处于 Public Preview。安全修复以尽力而为的方式应用到最新主分支和最新公开预发布版本。

| 版本 | 安全更新 |
| --- | --- |
| 最新主分支 | 支持 |
| v0.2.0-alpha.1 | 支持，直到下一预发布版本发布 |
| v0.1.0-alpha.1 | 不支持；请升级到最新预发布版本 |
| 更早的提交、分支或私有打包 | 不支持 |
| 第三方外置引擎 | 由各自上游负责 |

预览阶段不提供正式 SLA。发现高风险问题时，维护者可能先建议关闭相关模块、撤销凭据或停止使用受影响版本。

## 私下报告漏洞

请不要在公开 Issue、Discussion、Pull Request、日志片段或社交媒体中披露漏洞细节。

首选使用 GitHub 的 [Private vulnerability reporting](https://github.com/lt750965698-cyber/zhitai-workbench/security/advisories/new)。

如果该入口不可用，请只创建一个不含技术细节的公开 Issue，请求维护者建立私密联系方式。不要附上利用代码、真实路径、账号、Token、Cookie、二维码、平台数据或受影响人员信息。

私下报告建议包含：

- 受影响的标签、提交和运行环境；
- 攻击面、前置条件和最小复现；
- 机密性、完整性、可用性或账号风险；
- 是否可能绕过回环绑定、来源白名单、路径边界、可选签名校验、人工审核或发布确认；
- 已采取的临时缓解措施；
- 建议修复方式（如有）。

## 重点关注的安全问题

以下问题通常属于安全报告范围：

- 任意命令执行、参数注入或外置服务命令被浏览器输入控制；
- 路径遍历、符号链接逃逸、任意文件读写或媒体接口越界；
- Cookie、Token、密钥、二维码、手机号、私聊或原始素材泄漏；
- CORS、IPC、Webhook 签名、时间窗、nonce 或重放保护绕过；
- 未经确认的公开发布、账号切换、远程命令或批量操作；
- 本地节点被意外绑定到非回环地址或可被其他来源调用；
- 日志、导出、事件或错误信息中的敏感数据泄漏；
- 依赖安装或更新链路中的供应链风险。

以下情况通常不属于织台自身漏洞，但仍可作为普通问题报告：

- 外置引擎或平台本身的漏洞；
- 平台封号、验证码、接口变更、限流或服务条款争议；
- 在明确关闭安全边界后把本地节点暴露到公网；
- 需要已控制本地操作系统用户且没有扩大权限的行为；
- 内容版权、商标、肖像或授权争议。

第三方漏洞请优先报告给对应上游；如果织台的默认集成方式放大了风险，也请同时私下通知本项目。

## 协调披露

维护者会尽力确认报告、评估影响、准备修复并与报告者协调披露时间。请在修复或缓解措施可用前保密。修复发布后，项目可能公开致谢、公告和 CVE；是否署名由报告者决定。

## 安全部署基线

- 当前客户端只支持绑定 127.0.0.1；
- 不通过端口映射、隧道、反向代理或公网托管暴露本地节点；
- 外置引擎同样限制为回环访问，并使用最小文件与账号权限；
- 凭据使用系统钥匙串、权限受限的本地文件或短期环境变量；
- 不复用生产账号做测试；
- 公开发布和受管理服务使用现有确认门；对其他高影响写操作也应在流程中人工预览和确认，不要假设所有接口都已统一强制执行；
- 定期备份内容库，并在升级或迁移前验证恢复流程。

## Windows 预览版边界

- 支持 Windows 10 22H2 和 Windows 11 x64，但仍属于公开预览，不等同于已完成企业级安全评估；
- 预览安装包尚未代码签名，SmartScreen 可能显示“未知发布者”。只从 GitHub Releases 下载，并在运行前核对发布页面提供的 SHA-256；
- 原生第三方发布、外置服务自动安装或管理、微信自动化与浏览器自动化在 Windows 上失败关闭，并以稳定错误 `unsupported_on_windows_preview` 呈现；
- 不要因为桌面工作台可启动就绕过上述边界或手工启用未受支持的后台自动化。

更多背景见 [安全模型](docs/SECURITY_MODEL.md)。
