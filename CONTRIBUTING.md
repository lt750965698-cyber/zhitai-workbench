# 参与贡献

感谢你帮助改进织台。项目处于 Public Preview 阶段，优先接受能够提高可复现性、安全性、数据诚实性和人工可控性的贡献。

参与即表示你同意遵守 [社区行为准则](CODE_OF_CONDUCT.md)。

## 可以贡献什么

- 可复现的缺陷报告和回归测试；
- 中文文档、英文摘要、无障碍和易用性改进；
- 本地节点、知识库和桌面外壳的稳定性修复；
- 不携带第三方源码的适配器接口、模拟器和契约测试；
- 安全、隐私、凭据处理与人工审核门的改进；
- 对缺失能力诚实降级的实现，例如 unavailable、needs_attention 或 manual_review。

## 提交问题前

1. 搜索现有 Issues，确认没有重复问题。
2. 使用最新主分支或最新预发布版本复现。
3. 区分织台核心问题与外置引擎、平台登录或账号风控问题。
4. 删除日志中的 Cookie、Token、手机号、二维码、聊天内容、素材路径和其他个人数据。
5. 安全漏洞不要提交公开 Issue，请按 [安全政策](SECURITY.md) 私下报告。

## 本地开发

需要 Node.js 22.13.0 或更高版本和 pnpm 11.19.0 或更高版本。

~~~bash
git clone https://github.com/lt750965698-cyber/zhitai-workbench.git
cd zhitai-workbench
corepack enable
pnpm install --frozen-lockfile
cp local-agent/config.example.json local-agent/config.local.json
~~~

保持所有外置适配器关闭即可开发核心功能。分别启动本地节点和页面：

~~~bash
pnpm agent
pnpm dev
~~~

提交前运行：

~~~bash
pnpm lint
pnpm test
~~~

如果改动只影响文档，也应检查链接、命令、文件名和版本号是否与仓库一致。

## 分支与提交

- 从最新主分支创建短期分支；
- 每个 Pull Request 聚焦一个问题；
- 提交信息使用简洁的祈使语气，说明结果而不是过程；
- 不要重写无关文件，不要提交构建产物、本地数据库、账号状态或配置；
- 行为变化必须包含测试或解释为何无法自动测试。

## Pull Request 要求

请在模板中说明：

- 问题、方案和明确不在本次范围内的内容；
- 已运行的验证命令及结果；
- 对安全、隐私、数据迁移和人工审核门的影响；
- 是否新增网络请求、子进程、文件写入、外置依赖或平台动作；
- UI 变化的截图或录屏，且其中不得含真实账号或素材；
- 兼容性与回滚方式。

维护者可能要求拆分过大的改动、补充测试、更新文档或降低自动化权限。

## 第三方代码与集成

不要把来源不明或许可证不明确的源码、用户脚本、模型权重、媒体、字体、二进制或登录数据提交到仓库。

涉及外置引擎的贡献必须同时提供：

- 上游项目与官方地址；
- 固定版本、标签或提交；
- 许可证及再分发条件；
- 安装与升级是否会修改用户配置；
- 网络、文件、进程、账号和凭据权限；
- 失败、验证码、风控与人工降级路径；
- 可在无真实账号情况下运行的契约测试或模拟器。

若第三方许可证不明确，只能提交通用接口与配置文档，不得复制或改写其源码。尤其不要捆绑、分叉或发布没有明确再分发许可的浏览器脚本。

## 内容与数据

测试夹具必须是自行创建、可公开再分发或获得明确授权的合成数据。不要提交：

- 真实平台 Cookie、Token、二维码、手机号或账号标识；
- 私聊、评论、订单、客户资料或其他个人数据；
- 未授权的视频、音频、图片、逐字稿或平台导出；
- 内部交付报告、本机绝对路径、运行日志或真实样例 ID。

## 人工审核门

降低、绕过或默认关闭人工确认的改动通常不会被接受。涉及公开发布、账号操作、远程命令、批量抓取、模型生成或不可逆文件操作时，必须保留清晰的预览、范围说明、二次确认、审计记录和停止条件。

## 许可证

向本仓库提交代码，即表示你有权提交该内容，并同意该贡献按项目的 [MIT License](LICENSE) 提供。第三方内容仍受其原许可证约束，必须保留相应通知。
